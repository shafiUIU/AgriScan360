
# Laptop Server — AgriScan 360 dependencies
# Install with:  pip install -r requirements_laptop.txt

# FastAPI + Uvicorn (ASGI server)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0

# Pydantic (bundled with FastAPI, but pin for safety)
pydantic>=2.6.0

# Image processing
Pillow>=10.0.0
opencv-python-headless>=4.9.0   # headless = no GUI deps, works on server

# AI inference (ONNX Runtime — CPU)
onnxruntime>=1.18.0

# HTTP multi-part form handling (FastAPI uses python-multipart internally)
python-multipart>=0.0.9

# Utilities
python-dateutil>=2.8.2
aiofiles>=23.2.1                # Async file I/O for static serving
