
# Raspberry Pi 5 — AgriScan 360 dependencies
# Install on Pi with:  pip install -r requirements_pi.txt

# GPIO & Hardware
gpiozero>=2.0           # GPIO control (native RPi 5 / RP1 support)
lgpio>=0.2.2.0          # Required backend for gpiozero on RPi 5

# Camera
picamera2>=0.3.12       # Official RPi camera library for Camera Module 2

# Gas Sensor
bme680>=1.1.1           # Bosch BME680/BME688 I2C driver

# OLED Display
luma.oled>=3.13.0       # SSD1306 display library
luma.core>=2.4.2        # luma core rendering engine
Pillow>=10.0.0          # Image processing for OLED canvas

# HTTP Client (for uploading to laptop server)
requests>=2.31.0

# Utilities
python-dateutil>=2.8.2
