# =============================================================================
# Raspberry Pi 5 — AgriScan 360 Client Dependencies
# =============================================================================
# Install on Pi inside venv:  pip install -r requirements_pi.txt

# GPIO & Stepper Motor / MOSFET Control
gpiozero>=2.0
lgpio>=0.2.2.0; sys_platform == "linux"

# Gas Sensor (BME688)
bme680>=1.1.1
smbus2>=0.4.3

# OLED Display (SSD1306 0.96" I2C)
luma.oled>=3.13.0
luma.core>=2.4.2

# Image Processing & Produce Auto-Detection
Pillow>=10.0.0

# HTTP Client (Pi to Laptop Server communication)
requests>=2.31.0

# Utilities
python-dateutil>=2.8.2
